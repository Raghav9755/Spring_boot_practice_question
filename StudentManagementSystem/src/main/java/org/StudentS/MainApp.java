package org.StudentS;

import org.StudentS.model.StudentManager;

public class MainApp {
    public static void main(String[] args) {
        StudentManager studentManager = new StudentManager();

        // Create student objects and add to studentManager

        // Interaction with users: add, display, search students
    }
}